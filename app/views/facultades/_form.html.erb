<div class="block-form-content">
  <%= form_with(model: facultad, url: facultades_path(), method: :post) do |form| %>
    <% if facultad.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(facultad.errors.count, "error") %> prohibited this facultad from being saved:</h2>

        <ul>
          <% facultad.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :nombre %>
      <%= form.text_field :nombre %>
    </div>

    <div class="actions">
      <%= link_to 'Volver', facultades_path %> <%= form.submit "Crear facultad" %>
    </div>
  <% end %>
</div>