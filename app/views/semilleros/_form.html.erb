<div class="block-form-content">
  <%= form_with(model: semillero) do |form| %>
    <% if semillero.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(semillero.errors.count, "error") %> prohibited this semillero from being saved:</h2>

        <ul>
          <% semillero.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :nombre %>
      <%= form.text_field :nombre %>
    </div>

    <div class="field">
      <%= form.label :grupo_id %>
      <%= form.collection_select :grupo_id, grupos, :id, :nombre %>
    </div>

    <div class="field">
      <%= form.label :descripcion %>
      <%= form.text_field :descripcion %>
    </div>

    <div class="actions">
      <%= link_to 'Volver', semilleros_path %> <%= form.submit "Guardar" %>
    </div>
  <% end %>
</div>