<p id="notice"><%= notice %></p>
<div class="top-container">
    <h1>Semilleros</h1>
    <div class="actions">
      <div class="btn-group">
        <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Menú
        </button>
        <div class="dropdown-menu dropdown-menu-right">
          <%= link_to 'Crear Semillero', new_semillero_path, :class => "dropdown-item" %>
          <div class="dropdown-divider"></div>
        </div>
      </div>
    </div>
</div>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Grupo</th>
      <th>Descripcion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @semilleros.each do |semillero| %>
      <tr>
        <td><%= semillero.nombre %></td>
        <td><%= semillero&.grupo&.nombre %></td>
        <td><%= semillero.descripcion %></td>
        <td><%= link_to 'Ver', semillero, :class => "badge badge-info" %></td>
        <td><%= link_to 'Editar', edit_semillero_path(semillero), :class => "badge badge-secondary" %></td>
        <td><%= link_to 'Borrar', semillero, method: :delete, data: { confirm: 'Are you sure?' }, :class => "badge badge-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
